# Credenciais e dados pessoais - NUNCA commit
*.key
*.pem
*auth*.json
*.env
.env
data.sqlite
data.sqlite*
*.db
*.db-shm
*.db-wal
machine-id
jwt-secret
cli-secret
SUA_CHAVE*
*.secret
router.conf
# Banco local do 9Router (tem as chaves do usuario) - NUNCA commit
9Router/
data.sqlite
# Qualquer arquivo que possa conter chave de API real - NUNCA commit
*api-chave*
*api-key*
*apikey*
*api_keys*
api-keys.*
sk-or-*
AIza*
nvapi-*
sk-air-*
*.creds
# Chave PIX de doacoes (placeholder no README - preencher so na hora, nunca no git)
COLE_SUA_CHAVE*
PASTE_YOUR_PIX*

# Os de escrita
node_modules
package.json
package-lock.json
bun.lock
dist/
__pycache__/
.venv/
*.log
logs/
# Atalho local da maquina do autor (tem caminho pessoal)
*.lnk
# Executavel compilado (artefato de build - vai so para a pagina de releases)
KFAI-Instalador.exe
KFAI-Instalador.msi
# Build do MSI (WiX): payload copiado e Payload.wxs sao gerados pelo script
build/msi/staging/
build/msi/Payload.wxs
